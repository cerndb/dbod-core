use strict;
use warnings;

use Test::More;
use File::ShareDir;
use Data::Dumper;

require_ok('DBOD::DB');

subtest 'execute_sql_file' => sub {
    pass;
};

subtest 'select_sql_file' => sub {
    pass;
};

subtest 'do' => sub {
    pass;
};

done_testing();
